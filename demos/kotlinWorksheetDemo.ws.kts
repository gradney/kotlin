// A quick scratch from Kotlin.org tutorial
// ----
// A worksheet is basically a scratch sheet saved
// in a directory associated to a project module.
// Worksheets are saved; Scratches are not saved.
// The code will run as if it's in a main().
// ----
// Can use classes or functions from a Kotlin project,
// in our scratches and worksheets.

val x = 2
val y = 3

var i = x + y
while (i++ < 8)

    println(i)

println("Hello World")